import { Module } from '@nestjs/common';

/**
 * Feature `property` (rental-flow, fase 1: Listado).
 *
 * Expone el dominio (entidades + puertos). Los casos de uso
 * (application) y los adaptadores (infrastructure) se agregan en
 * issues derivados; este módulo solo registra el feature en el
 * contenedor de NestJS.
 */
@Module({})
export class PropertyModule {}
